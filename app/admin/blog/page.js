import { BlogAdmin } from "@/components/admin/blog-admin"

export const metadata = {
  title: "Blog Admin | Content Management",
  description: "Manage blog posts, create new content, and publish articles.",
}

export default function BlogAdminPage() {
  return <BlogAdmin />
}
